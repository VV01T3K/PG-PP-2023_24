#include <stdio.h>

int main() {
    // szesnastkowo ascii inputa
    int c;
    // while (c = fgetc(stdin), c != EOF) {
    //     printf("%x\n", c);
    // }
    // -------------------------------------
    // while ((c = fgetc(stdin)) != EOF) {
    //     printf("%x\n", c);
    // }

    // int a = (2, 3);
    // int b, c;
    // int a = (b = 2, c = 5, c + b);
    // printf("%d\n", a);

    return 0;
}

// midnight commander do podglądy plików